# Copy this file to `.env` for local development, then fill in your own values.
# Prefix with VITE_ so Vite exposes the value to the client bundle.
# On Vercel, add the same variable under Project Settings > Environment Variables.
VITE_MAPBOX_API_KEY=your-mapbox-public-token-here
